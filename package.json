{
  "name": "hasanwahab-portfolio-backend",
  "version": "1.0.0",
  "private": true,
  "description": "REST API for portfolio JSON — admin web app updates; Flutter app reads.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  }
}
